# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/elitechatapi

# Session Configuration (Generate a strong random string)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Server Configuration
PORT=5000
NODE_ENV=production

# Development Settings (Optional)
# NODE_ENV=development

# Note: WhatsApp Business API settings are configured through the Settings page in the application
# No environment variables needed for WhatsApp API - all managed through the UI